// HeroUI's Tailwind plugin, loaded from globals.css via `@plugin './hero.ts'`
// (Tailwind 4 loads JS plugins through CSS rather than tailwind.config.ts).
import { heroui } from "@heroui/theme";

export default heroui();
